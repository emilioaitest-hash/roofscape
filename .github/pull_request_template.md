## What changed

<!-- One paragraph. What behaviour is different now than before? -->

## Why

<!-- The problem. If this fixes something, what went wrong and how was it found? -->

## What it costs

<!-- Every change buys something and costs something. Name the cost: a new
dependency, a slower path, a case now unhandled, a decision deferred. "Nothing"
is a valid answer but should be a considered one. -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] Behaviour changed on purpose is covered by a test
- [ ] A decision with a real alternative is recorded in `docs/decisions/`
