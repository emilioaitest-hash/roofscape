# One of these is enough. The container cannot use a Claude Code subscription —
# there is no binary in it and no way to log one in — so it needs a key.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# The port published on the host's loopback interface.
ROOFSCAPE_PORT=7717
